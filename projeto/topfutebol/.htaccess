RewriteEngine On

# 1. Redirecionar externamente /arquivo.html para /arquivo
# Isso garante que se alguém digitar .html, o navegador muda a URL
RewriteCond %{THE_REQUEST} \s/+(.+?)\.html[\s?] [NC]
RewriteRule ^ /%1 [R=301,L]

# 2. Redirecionar internamente /arquivo para /arquivo.html
# O navegador mostra sem extensão, mas o servidor lê o arquivo .html
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.+)$ $1.html [L]